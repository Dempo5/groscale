// src/components/UploadWizard.tsx
import React from "react";

/**
 * Legacy shim: we moved the wizard into pages/Uploads.tsx.
 * This blank component prevents TS/build errors if anything still imports it.
 */
export default function UploadWizard() {
  return null;
}
